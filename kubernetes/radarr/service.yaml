---
# Radarr Service Manifest
# Exposes Radarr internally within the cluster via ClusterIP.
apiVersion: v1
kind: Service
metadata:
  name: radarr
spec:
  type: ClusterIP
  selector:
    app: radarr
  ports:
    - port: 7878
      targetPort: 7878
