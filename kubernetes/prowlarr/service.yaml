---
# Prowlarr Service Manifest
# Exposes Prowlarr internally within the cluster via ClusterIP.
apiVersion: v1
kind: Service
metadata:
  name: prowlarr
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: prowlarr
  ports:
    - port: 9696
      targetPort: 9696
