---
name: Homelab CI
on:
  push:
    branches:
      - main

jobs:
  scan:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Write config files
        run: |
          echo "${{ secrets.ANSIBLE_CFG }}" > ansible/ansible.cfg
          echo "${{ secrets.INVENTORY }}" > ansible/homelab-nodes

      - name: Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Ansible collections
        run: ansible-galaxy collection install community.crypto

      - name: Ansible Lint
        uses: ansible/ansible-lint@v26.3.0
        with:
          requirements_file: ansible/requirements.yml
        env:
          ANSIBLE_COLLECTIONS_PATH: /usr/lib/python3/dist-packages/ansible_collections

      - name: YAML Lint
        uses: ibiqlik/action-yamllint@v3
